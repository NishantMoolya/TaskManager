import { createTheme } from "@mui/material";

const theme = createTheme({
    palette:{
        primary:{
            main:"#2196f3"
        },
        secondary:{
            main:"#00b0ff"
        }
    }
})

export default theme;